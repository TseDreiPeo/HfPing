<Page
    x:Class="APureUwpApp.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:APureUwpApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <ScrollViewer HorizontalScrollMode="Enabled">
            <TextBlock Name="MyConsoleTxtBlock" IsTextSelectionEnabled="True" />
        </ScrollViewer>
        <StackPanel Grid.Row ="2" Orientation="Horizontal">
            <Button Content="Clr" Click="Button_Click_2" />
            <Button Content="UpdateTile manual" Click="Button_Click" />
            <Button Content="Log A line" Click="Button_Click_1" />
        </StackPanel>

    </Grid>
</Page>
